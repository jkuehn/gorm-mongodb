h1. deleteAll

h2. Purpose

Returns the "Datastore":http://code.google.com/p/morphia/source/browse/trunk/morphia/src/main/java/com/google/code/morphia/Datastore.java instance for low level operations

h2. Examples

{code:java}
// everything
Book.getDatastore()
{code}

h2. Note

This method is likely to be removed in future in favor of a service that provides access to the configured Mongo, DB, Morphia and Datastore instances.