h1. Database Mapping

You can use the @mapping@ block to customize the ORM mapping behavior.

{code:java}
class Book {
	String title
	Date releaseDate
	Author author
	
	static mapping = {
		author index:"auth_idx"
	}
}
{code}

